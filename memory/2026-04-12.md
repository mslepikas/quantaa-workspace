# 2026-04-12 - Linux System Monitoring Dashboard

## Context
- User Mark has 4 monitors: GeekPi 7.84" (rack), Dell 21" vertical, 2x Anyuse 15.6"
- Goal: Build professional Linux system monitoring dashboard
- Tech stack: FastAPI + React + Tailwind + shadcn/ui + recharts
- Running locally on port 8000

## Completed Tasks
1. ✅ Built production-ready Linux monitoring dashboard
2. ✅ FastAPI backend with endpoints:
   - `/api/system` - Full system info
   - `/api/cpu` - CPU usage per core
   - `/api/memory` - RAM usage with breakdown
   - `/api/disk` - Disk I/O and mount points
   - `/api/network` - Network interfaces, throughput
   - `/api/temperature` - System temperatures
   - `/api/gpu` - GPU info via nvidia-smi
   - `/api/processes` - Top processes
   - `/api/uptime` - System uptime
3. ✅ React frontend with charts (recharts)
4. ✅ Systemd service file created
5. ✅ Install script created
6. ✅ Dashboard running on `http://localhost:8000`

## Dashboard URL
- Local: `http://localhost:8000`
- Network: `http://YOUR_IP:8000`

## Lessons Learned
- Must actually execute file writes, not just describe them
- Keep responses concise and action-oriented
- Avoid repetitive explanations without action
- User wants actual work done, not circular responses

## Completed Later Today
7. ✅ Fixed GeekPi 4-monitor hardware setup by switching one connection to an HDMI-to-DP cable
8. ✅ Confirmed working display layout via `xrandr`: USB-C-2.3, HDMI-0, USB-C-0, and USB-C-2.1 connected; HDMI-0 primary; total desktop 4920x1920
9. ✅ Got the simpler GeekPi website loading locally from `~/geekpi-dashboard`
10. ✅ Moved dashboard web server to port `8081` after port conflict on 5000
11. ✅ Wrote a black/orange themed `index.html` for the dashboard at `~/geekpi-dashboard/index.html`
12. ✅ Reviewed OpenClaw model config and confirmed OpenAI provider is configured in `~/.openclaw/openclaw.json`
13. ⚠️ Config was temporarily switched to OpenAI primary (`openai/gpt-5.4`), but user preference is hybrid: keep Ollama for local/default work and use OpenAI only as needed for web design help

## Notes
- Dashboard server file at `~/geekpi-dashboard/server.js` still serves inline HTML and does not yet read the themed `index.html`, which caused part of the styling confusion
- User wants less back-and-forth and more first-pass reliability on web work

## Next Steps
- Reconcile `~/geekpi-dashboard/server.js` so it serves `index.html`
- Restore `~/.openclaw/openclaw.json` to Ollama as primary default model
- Keep OpenAI configured and available for selective use on web design tasks, because local Ollama web-building results were not good enough
- Decide on a clean workflow for when to use OpenAI versus local models
- Add more chart components to frontend
- Integrate real API calls to fetch live data
- Set up systemd service for auto-start
- Add process monitoring
- Implement temperature alerts
