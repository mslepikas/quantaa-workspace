# 2026-04-13

- Built and debugged `linux-monitor-system` in workspace with FastAPI backend and React frontend.
- Important bug fixed: static frontend routing was swallowing `/api/*` routes, causing dashboard fetch failures and 404s.
- Important bug fixed: memory API crashed due to `svmem.slabs`; corrected to use `slab` with safe `getattr` handling.
- Reworked dashboard UI for a small rack display target around 400x400 resolution.
- Added compact live dashboard layout with CPU/RAM gauges, CPU and memory trend graphs, mini stats, and primary network summary.
- Added GPU and temperature gauge support to the compact dashboard, plus improved `nvidia-smi` parsing in `api/gpu.py`.
- User wants the Linux monitoring page to show real live metrics and fit well on a small 10-inch rack monitor.
- Interaction lesson: user was frustrated by repeated claims of completion before the page actually worked; future work should verify before claiming success.
- Confirmed final OpenClaw model config is hybrid: local model first, OpenAI available as fallback/selective use.
- Current `~/.openclaw/openclaw.json` model setting: `primary = ollama/qwen3:14b`, `fallbacks = ["openai/gpt-5.4"]`.
- User wants Ollama/local models for normal work, with OpenAI only when local web-building quality is not good enough.
- Saved working rule: use local Ollama by default, switch to OpenAI for explicit requests or for web design/UI polish when local output is weak, then return to local-first after that task.
- Saved shorthand: if Mark says "big brain," that means use OpenAI for that task.
- Added health-check trigger phrase: "How do you feel, Angus?" means run the local health check and report status.
# 2026-04-13

- Built and debugged `linux-monitor-system` in workspace with FastAPI backend and React frontend.
- Important bug fixed: static frontend routing was swallowing `/api/*` routes, causing dashboard fetch failures and 404s.
- Important bug fixed: memory API crashed due to `svmem.slabs`; corrected to use `slab` with safe `getattr` handling.
- Reworked dashboard UI for a small rack display target around 400x400 resolution.
- Added compact live dashboard layout with CPU/RAM gauges, CPU and memory trend graphs, mini stats, and primary network summary.
- Added GPU and temperature gauge support to the compact dashboard, plus improved `nvidia-smi` parsing in `api/gpu.py`.
- User wants the Linux monitoring page to show real live metrics and fit well on a small 10-inch rack monitor.
- Interaction lesson: user was frustrated by repeated claims of completion before the page actually worked; future work should verify before claiming success.
- Confirmed final OpenClaw model config is hybrid: local model first, OpenAI available as fallback/selective use.
- Current `~/.openclaw/openclaw.json` model setting: `primary = ollama/qwen3:14b`, `fallbacks = ["openai/gpt-5.4"]`.
- User wants Ollama/local models for normal work, with OpenAI only when local web-building quality is not good enough.
- Saved working rule: use local Ollama by default, switch to OpenAI for explicit requests or for web design/UI polish when local output is weak, then return to local-first after that task.
- Saved shorthand: if Mark says "big brain," that means use OpenAI for that task.
- Added health-check trigger phrase: "How do you feel, Angus?" means run the local health check and report status.
- Fixed GeekPi 4-monitor display setup with xrandr (USB-C-2.3, HDMI-0, USB-C-0, USB-C-2.1) and HDMI-to-DP cable swap for one path.
- Verified Linux monitor app on port 8000 is healthy and added to health check script.
- Reset Telegram session model fallback from gpt-5.4 back to local qwen3:14b default.
- Configured model prioritization strategy: phi3:latest → qwen3.5:latest → qwen3:14b → llama3.2:latest → mistral:latest → nemotron-3-super:120b → openai/gpt-5.4
- Updated OPENCLAW config to set model fallback chain in order for intelligent use of resources and speed optimization.
- Added model ranking for clarity: fastest (phi3), medium (qwen3.5), large (qwen3:14b), largest (nemotron-3-super).
